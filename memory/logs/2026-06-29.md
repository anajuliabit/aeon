# 2026-06-29

## External Feature
- **Repo:** aaronjmars/aeon
- **What:** Wired the existing `scripts/validate-config.test.js` suite (7 fixture tests for the checkout-ordering invariant, added with #546) into `.github/workflows/ci-tests.yml`. The test file shipped but was never invoked by CI, so a regression to `analyzeCheckout` would land silently. One-line CI step alongside the other `scripts/tests/*` runners; `node --test` is built into the Node 20+ already on `ubuntu-latest`. Verified suite passes 7/7 locally on current `main`.
- **PR:** https://github.com/aaronjmars/aeon/pull/560
- **Branch:** `ai/ci-validate-config-tests` (pushed via fork `anajuliabit/aeon-fork` — `anajuliabit/aeon` already exists as an unrelated repo, so the fork uses a unique name)
- **Why:** Proactive gap-fix — `validate-config.js` is the structural validator the `config-validator` skill leans on; its tests existed but had no CI gate.
