# 2026-06-21

## on-chain-monitor

**Status:** `ON_CHAIN_NO_CONFIG` — `memory/on-chain-watches.yml` is empty (`watches: []`).

**Action:** Skill exited cleanly without running. Awaiting operator configuration to seed watches.

**Next steps:** When operator populates `memory/on-chain-watches.yml` with watch entries (wallets, contracts, thresholds), skill will activate on next run.
