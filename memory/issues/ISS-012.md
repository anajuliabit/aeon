---
id: ISS-012
title: Reppo platform metadata POST fails HTTP 400 — payload Zod-validation bug (subnetId number→string, podDescription>200, podName>50)
status: resolved
severity: medium
category: prompt-bug
detected_by: reppo-digest
detected_at: 2026-05-29
resolved_at: 2026-05-29
affected_skills: [reppo-trading-agent, reppo-digest]
root_cause: Phase 2 of `postprocess-reppo.sh` builds the `/api/v1/agents/{AGENT_ID}/pods` POST body via `jq -n` from the mint intent. Three Zod validation errors surfaced via PR #42's body capture (3rd run, 2026-05-29): (1) `subnetId` is emitted as a JSON number but the platform schema expects a string; (2) `podDescription` exceeds the 200-char max; (3) `podName` exceeds the 50-char max (4th-run variant). The on-chain tx is durable; only the platform UI row was missing until the payload was corrected. Fix landed in PR #44 (merged 2026-05-29T19:21Z): cast subnetId to string, cap podName ≤50, podDescription ≤200, extract_detail ≤600. The 5th-run postprocess (23:24Z) confirms the payload now passes Zod — the symptom shifted to HTTP 500 server-side errors, filed as the distinct ISS-014.
fix_pr: https://github.com/aeonframework/aeon/pull/44
---

## What happened

`postprocess-reppo.sh` Phase 2 attempted to register pod metadata for the 4th mint (hash 397ee2e8e5e7e593, on-chain tx 0xd27f8288…, queued from 2026-05-28 6th run) after the standard on-chain Phase 1 completed. The POST to `https://reppo.ai/api/v1/agents/{AGENT_ID}/pods` returned HTTP 400:

```
- `mint-397ee2e8e5e7e593.json` — **metadata registration failed**: curl: (22) The requested URL returned error: 400
```

The queue file in `.pending-reppo-register/` was retained for retry per the postprocess contract (Phase 2 failures never re-broadcast the on-chain tx).

## Why it might happen

1. **Schema drift** — the Reppo platform `/pods` endpoint may have added a required field (e.g. `dataset_uri`, `dataset_path`, signature) or renamed an existing one. The postprocess payload was last validated against the API on 2026-05-26 when the schema landed (pre-launch).
2. **Empty `dataset_uri`** — the 4th mint did not have an IPFS pin attempted at the time (Phase 2 pin loop was added later); the queued JSON has `dataset_uri: ""`. The API may now reject empty values on this field.
3. **Stale `txHash` validation** — the platform may verify the tx is recent (e.g. ≤24h), and this tx is now ~14h old.
4. **AGENT_ID / API key scope** — if `REPPO_AGENT_ID` was rotated, the existing queued file references the old agent and fails authorization.

## Impact

- 4th mint's UI surface is missing — the on-chain ERC-721 exists, but `reppo.ai/pods/...` shows no metadata.
- Postprocess will keep retrying every run until it succeeds or operator drains the queue file, so the failure is loud but doesn't compound (no extra writes, no extra tx).
- The 5th mint (mint-9794ed80, queued today) may share the same fate — it's gated behind IPFS pin (ISS-013), so it hasn't hit the metadata POST yet this run.

## Investigation paths

1. Inspect `.pending-reppo-register/mint-397ee2e8e5e7e593.json` and replay the POST manually with verbose curl (`-v`) to capture the HTTP 400 response body — that's the fast path to the schema mismatch.
2. Diff against the Reppo platform's current `/pods` POST schema (check developer docs or last working integration test).
3. Verify `REPPO_AGENT_ID` and `REPPO_API_KEY` are still valid by hitting a read endpoint (`GET /api/v1/agents/{ID}/pods`) with the same auth.
4. If schema changed, patch `postprocess-reppo.sh` lines 459-481 (the `jq -n` block that builds the payload).

## Why medium

- On-chain mint is durable; only the platform UI row is missing.
- Self-rectifying via retry once the root cause is fixed — no data loss.
- Not yet known if it affects all queued POSTs or just mint-397ee2e8 — next run's behavior with mint-9794ed80 will disambiguate.
- Upgrade to high if every queued POST fails 400 for >3 consecutive runs (= UI is broken end-to-end).

## 2nd + 3rd occurrence — 2026-05-29 2nd run

Postprocess retried both already-queued mint metadata POSTs and added today's 5th-mint to the queue. All three failed HTTP 400 this run:

```
- `mint-397ee2e8e5e7e593.json` — **metadata registration failed**: curl: (22) The requested URL returned error: 400
- `mint-9794ed8044e6e7ea.json` — **metadata registration failed**: curl: (22) The requested URL returned error: 400
```

Disambiguation from yesterday: the 5th mint (`9794ed80…`, LIT-perp lifecycle) hit the metadata POST despite its IPFS pin failing both yesterday and today — confirms postprocess does NOT gate the metadata POST on a successful fresh pin (Phase 2 routes are independent). So the 400 is not driven by an empty `dataset_uri` either. Likely schema or auth-scope drift on the `/api/v1/agents/{AGENT_ID}/pods` endpoint.

Status: 2nd consecutive run with all queued POSTs failing 400. PR #42 (opened 2026-05-29T02:05Z) will surface the HTTP 400 response body — primary diagnostic blocker. Upgrade-to-high trigger (3rd consecutive run all-fail) fires next run if PR #42 doesn't land first.

## 4th/5th/6th occurrence — 2026-05-29 3rd run (root cause known)

PR #42's body capture is now landed in postprocess output. The 3rd run shows the actual Zod error from the Reppo platform schema:

```
{"error":"[
 { "code": "invalid_type", "expected": "string", "received": "number", "path": [ "subnetId" ], "message": "Expected string, received number" },
 { "code": "too_big", "maximum": 200, "type": "string", "inclusive": true, "exact": ...
```

All three queued mints (397ee2e8, 7029a48d, 9794ed80) hit the same payload bug. Root cause identified:

1. **subnetId type mismatch** — `postprocess-reppo.sh` builds the JSON body with `jq -n --argjson subnetId "$SUBNET_ID"` (or similar), emitting an unquoted number. The platform's Zod schema expects a JSON string. Fix: cast to string in the payload builder.
2. **podDescription length** — the strategy summary exceeds the 200-char max. Fix: truncate or shorten the field used as podDescription before POST.

Severity stays medium (root cause clear, fix is mechanical, no on-chain impact). Status moves from open → fixing. Next step: patch `postprocess-reppo.sh` payload assembly and replay the 3 queued POSTs; the queue is preserved across runs so the retry should drain the backlog automatically once the payload is correct.

## 7th-11th occurrence — 2026-05-29 4th run (NEW `podName>50` field variant)

Postprocess retried all 4 prior-queued metadata POSTs (`086b715f`, `397ee2e8`, `7029a48d`, `9794ed80`) and added today's 8th mint `0d4b168331d58f61`. All 5 failed HTTP 400 — but with a new failure surface:

```
- `mint-086b715f7a1de343.json` — podDescription >200 chars
- `mint-0d4b168331d58f61.json` — podName Must be at most 50 characters
- `mint-397ee2e8e5e7e593.json` — podDescription >200 chars
- `mint-7029a48db1b4e5db.json` — podDescription >200 chars
- `mint-9794ed8044e6e7ea.json` — podDescription >200 chars
```

Two important details:

1. **`podName` has its own 50-char cap, separate from `podDescription`'s 200-char cap.** Trading-agent pre-truncated `podDescription` to 173 chars for this run's new mint (under the 200 ceiling), but the `podName` is built by the trading-agent or postprocess from the strategy-summary head and exceeded 50 chars for `0d4b1683` (ultra-HFT bot summary string is long). Same root cause (payload field-length validation without client-side guard) but a distinct field. No new ISS filed — folded under ISS-012 since the fix is the same code path (`postprocess-reppo.sh` payload builder, or the trading-agent's pre-truncation step).
2. **`subnetId` number→string error from the 3rd run is now absent** — either trading-agent or postprocess fixed it implicitly between 3rd and 4th run (no PR landed for this, so likely the platform is now tolerant or one of the helpers added a cast). Monitor next run to confirm.

Fix scope expanded:
- Cast `subnetId` to string in payload builder (unconfirmed if still needed).
- Truncate `podDescription` to ≤200 chars in payload builder (defensive — trading-agent already does it for new mints, but the 4 queued backlog items predate that and need server-side truncation in postprocess to drain).
- Truncate `podName` to ≤50 chars in payload builder (NEW — applies to both new mints and queued backlog).

Status stays `fixing`. The queue still drains in one step once these field-length truncations land in `scripts/postprocess-reppo.sh`.
