---
id: ISS-009
title: reppo-trading-agent SKIPs when orchestrator chain output lacks fenced reppo-plan block
status: open
severity: medium
category: output-format
detected_by: reppo-digest
detected_at: 2026-05-26
affected_skills: [reppo-orchestrator, reppo-trading-agent, reppo-digest]
root_cause: On the 18th reppo-swarm run today, the chain-passed `.outputs/reppo-orchestrator.md` contained only a `## Summary` section — the fenced ```reppo-plan``` block with `reppo-trading-agent: datanet 9` (which trading-agent gates on in Step 1) was absent. The orchestrator's summary explicitly states "produced the RUN/SKIP plan with one assigned agent (reppo-trading-agent → datanet 9)", so the orchestrator skill logically ran the plan; the loss is somewhere in the orchestrator's emitted output or the chain-runner's capture. The 17th run earlier today wrote the fenced block correctly (parsed fine, mint landed on-chain), so this is intermittent — not a permanent format regression.
fix_pr: null
---

## What happened

Today's 18th `reppo-swarm` chain run dispatched. `reppo-orchestrator` ran
and recorded a clean `1 RUN / 0 SKIP` in `memory/logs/2026-05-26.md` and
in its own `## Summary`. But the chain-passed body in
`.outputs/reppo-orchestrator.md` contains *only* the Summary section —
no fenced ```reppo-plan``` block.

`reppo-trading-agent` runs next, gates on the fenced block per its
SKILL.md Step 1, and SKIPs:

```
Skipped: no orchestrator plan available
```

No intents queued, no `## Execution Results` section, no on-chain
activity. The 17th run earlier today (5th of today's reppo-swarm runs)
had the block parse correctly — the mint
`0x77f1386f…e48a61f` landed (first on-chain ever) on that run. So the
orchestrator's output format isn't permanently broken; this is an
intermittent drop on chain hand-off.

## Impact

- 1 entire reppo-swarm run lost (0 minted intents queued, 0 votes
  considered). For datanet 9 the practical impact is small because the
  mint cap is already saturated (1 minted today), but on a run where
  votes could land (post-ISS-006), losing the body would cost the
  vote slot.
- Pattern repeats undetected unless reppo-digest specifically calls it
  out — orchestrator + trading-agent both look "successful" in
  cron-state since each individual skill exits clean.

## Fix

Two investigation paths:

1. **Inspect the orchestrator's actual stdout.** Compare the
   17th-run vs 18th-run orchestrator output in the GitHub Actions logs.
   If the orchestrator only emitted the Summary on this run, the
   skill is at fault (likely an early-return path or LLM omission
   under retry). Tighten the SKILL.md to require the fenced block
   before the Summary, or have it write the block to a sidecar file
   (e.g., `.outputs/reppo-orchestrator.plan.txt`) that the chain
   runner copies verbatim.
2. **Inspect chain-runner output capture.** If the orchestrator's
   raw output *did* contain the fenced block but the version saved
   to `.outputs/` was truncated, the bug is in the chain-runner step
   that writes `${skill}.md`. Check `.github/workflows/chain-runner.yml`
   for any post-processing (head/tail truncation, character limits,
   whitespace stripping) that could strip a fenced block.

A defensive fix on the consumer side: have reppo-trading-agent
re-derive the plan from the Summary text if the fenced block is
absent (it can read "reppo-trading-agent → datanet 9" from the
Summary), but this masks the upstream bug rather than fixing it,
so prefer the orchestrator-side fix once located.

## Why medium (not high)

- Intermittent: 1 of 5 runs today (20%). Not at >50% threshold.
- The orchestrator + trading-agent + digest each still exit clean
  individually — no cascade failure.
- The first on-chain mint already landed earlier today (17th run),
  so the chain isn't dormant.
- Will revisit severity if recurrence rate climbs above 50%.
