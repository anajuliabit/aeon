---
id: ISS-031
title: usepod.ai LLM proxy 402 Payment Required — fleet-wide dispatch failure 2026-08-10 (ISS-029 recurrence)
status: open
severity: critical
category: missing-secret
detected_by: skill-health
detected_at: 2026-08-10T19:14:12Z
resolved_at: null
affected_skills: [btc-levels, cost-report, thought-review, daily-routine, skill-freshness, weekly-shiplog, heartbeat, security-digest, morning-brief, github-trending, unlock-monitor, token-alert, deal-flow, goal-tracker, list-digest, skill-security-scan, action-converter, reflect, skill-health, agent-buzz, search-skill]
root_cause: |
  api.usepod.ai/v1/messages returns HTTP 402 to every fleet dispatch on 2026-08-10.
  Error payload: `"error":"Payment required. Retry the same request with an X-PAYMENT or PAYMENT-SIGNATURE header."`
  aeon.yml gateway.provider = "direct" (CLAUDE_CODE_OAUTH_TOKEN) but dispatches are
  hitting usepod.ai — either the OAuth token quota is exhausted and the harness
  silently falls back to usepod, or the provider config drifted.
related: ISS-029
fix_pr: null
---

## What happened

Every scheduled skill fired 2026-08-10 15:10Z → 18:03Z lands the same signature —
`api.usepod.ai/v1/messages 402 Payment required`. 16 skills crossed the CRITICAL
consec≥3 gate in a single day (yesterday 8-09 18:17Z snapshot: 0 CRITICAL). Same
error string as ISS-029 (2026-08-03), which self-healed inside ~2h. Today's window
is ≥3h and still failing at check time.

## Signal

- CRITICAL cohort (consec, sr): btc-levels (17, 0.79), cost-report (15, 0.09),
  thought-review (15, 0.63), daily-routine (14, 0.63), skill-freshness (13, 0.60),
  weekly-shiplog (12, 0.33), heartbeat (10, 0.69), security-digest (8, 0.42),
  morning-brief (7, 0.69), github-trending (7, 0.65), unlock-monitor (7, 0.38),
  token-alert (6, 0.80), deal-flow (6, 0.44), goal-tracker (3, 0.53),
  list-digest (3, 0.58), skill-security-scan (3, 0.67)
- WARNING (consec 1-2, same signature): agent-buzz (2, 0.68), search-skill (2, 0.37),
  action-converter (1, 0.54), reflect (1, 0.56), skill-health (1, 0.52)
- Dispatch window tight: 15:10Z → 18:03Z cluster (single ~3h wave), same as
  ISS-029's 11:49Z → 18:27Z shape
- last_error signature IDENTICAL across all affected skills (byte-for-byte usepod 402 payload)
- Only clean successes today: none observed in cron-state through 18:03Z

## What was NOT affected

This skill-health run itself succeeded — manually invoked via Claude Code CLI in
the sandbox, does not route through usepod.ai. Same escape hatch ISS-029 used.

## Fix ownership

Operator-gated. Two candidate paths (from ISS-029 postmortem):

1. Top up usepod.ai payment or add the `X-PAYMENT`/`PAYMENT-SIGNATURE` header the
   endpoint now requires (temporary — will re-fire when quota drains again).
2. Rotate fleet to backup LLM proxy — flip `gateway.provider` in aeon.yml from
   `direct` to `bankr` or `virtuals`, or investigate why `direct` is silently
   routing through usepod despite the config.

Aeon cannot self-repair — failure surface is the LLM proxy Aeon runs on.

## Related

- **ISS-029** — original 2026-08-03 event, resolved same-day via usepod self-heal.
  This is the recurrence (per skill-health constraint: previously-resolved issues
  that re-fire get a new ISS with a pointer, not a re-open).
- ISS-030 — cost-report's chronic `sdk_opt_in_required` signature. Today
  cost-report is failing with the ISS-031 usepod 402 signature instead, so its
  Mon 8-10 07Z deciding-test is inconclusive (see ISS-030 update note).
- ISS-028 — bash `>` redirect sandbox family. Some overlap in affected_skills but
  distinct root_cause.
