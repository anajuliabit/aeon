# Vuln Scan — vercel/eve · 2026-06-21

## TL;DR

**scan skipped.** scanner binaries (semgrep, trufflehog, osv-scanner) blocked by sandbox due to missing `scripts/prefetch-vuln-scanner.sh` (ISS-018). Manual security posture review found no obvious vulnerabilities in source code. Dep-CVE web search rate‑limited (GH API 404 on advisories endpoint). No actionable findings to disclose via PVR or public PR.

## Repo metadata

| Field | Value |
|---|---|
| Repo | [vercel/eve](https://github.com/vercel/eve) |
| Stars | 1,950 (at scan time) |
| Primary language | TypeScript |
| License | Apache-2.0 |
| Last push | 2026-06-21T13:15Z |
| Pinned commit | clone `--depth 200` from `main` |
| `SECURITY.md` | yes — `responsible.disclosure@vercel.com` email contact |
| PVR status | unknown (graphql query 404) |
| Workspaces | `packages/eve` (main), `apps/*`, `e2e/fixtures/*` |
| Package manager | pnpm 9.12.0 (lockfile: 989kB, ~1,000 resolved packages) |
| Touch surfaces | agent auth, sandbox execution, MCP client connections |

## Scanner sources

| Scanner | Status | Notes |
|---|---|---|
| semgrep (SAST) | `fail` | `pip install semgrep` blocked by sandbox — ISS-018. |
| trufflehog (verified secrets) | `fail` | Tarball download/extract blocked by sandbox — ISS-018. |
| osv-scanner (deps) | `fail` (binary) → `manual` (attempted API) | Binary download blocked; `GET /advisories?ecosystem=npm` returned 404 (graphql?); web search rate‑limited. |
| slither (Solidity) | `n/a` | No `.sol` files in repo. |

**Coverage caveat:** no code‑flow or verified‑secret scanning occurred.

## Manual triage

### Dependency CVEs

Lockfile shows `axios@1.16.1`, `jose@6.2.3`, `chokidar@5.0.0`, `semver@7.8.4`, `turndown@7.2.4`, `jsonc-parser@3.3.1`. All are recent versions (post‑2024). Quick web‑search for 2025‑2026 CVEs yielded no immediate hits but GH Advisory API 404 limited deeper investigation.

### Code‑flow review

Examined for:
- **Command injection:** `child_process.spawn` appears only in setup scripts (`spawnPackageManager`, `open‑url`) with static/hardened arguments.
- **Path traversal:** No file‑system writes observed in reviewed sources (sandbox code uses virtual filesystem).
- **Auth bypass:** Agent auth hooks (`bearer`, `basic`, `vercelOidc`) correctly tokenize credentials.
- **SSRF:** Remote agent dispatch appears HTTPS‑only; no raw URL fetches found.
- **Deserialization:** No `eval`, `Function`, `vm.runInContext` usage in runtime code.
- **Secrets in git history:** Could not scan — trufflehog missing.

Findings: **no obvious high‑severity code flaws** in reviewed surfaces (`packages/eve/src/public/sandbox/`, `packages/eve/src/public/agents/auth.ts`, `packages/eve/src/public/channels/`).

### Credential handling

- `@vercel/oidc` token looked up via `getVercelOidcToken` → stored in env var `VERCEL_OIDC_TOKEN`.
- `bearer(token)` and `basic({username, password})` accept `TokenValue` (string or async function) — no hard‑coded secrets seen.
- No plain‑text credentials committed in scanned files.

## Confirmed findings

**0.** No PVRs filed, no PRs opened.

## Disclosure channel eligibility

- **PVR** (GitHub private advisory): unknown status (graphql query 404).
- **SECURITY.md contact:** `responsible.disclosure@vercel.com` available.
- **Action:** With zero confirmed findings, no disclosure needed.

## Dedup state

Appended to `memory/vuln‑scanned.json`:

```json
{
  "repo": "vercel/eve",
  "scanned_at": "2026-06-21T13:28:00Z",
  "findings": 0,
  "channel": "skipped-no-scanner",
  "scanner_status": {
    "semgrep": "fail",
    "trufflehog": "fail",
    "osv-binary": "fail",
    "osv-api": "tool-not-available",
    "slither": "n/a"
  },
  "notes": "manual audit only — scanner binaries missing (ISS-018). Examined dependency tree (axios 1.16.1, jose 6.2.3, etc.) for known CVEs via manual web search but rate‑limited; basic security posture review found no obvious code-flow vulnerabilities. SECURITY.md present with PVR email contact."
}
```

30‑day skip armed for this repo. Next eligible scan window opens 2026‑07‑21.

## Follow‑ups

1. **ISS-018** — still open, blocking SAST + secrets + dep‑CVE scanning across all vuln‑scanner runs.
2. **Prefetch script** — required before the skill can achieve full coverage. Currently manual web search for advisories is rate‑limited and unreliable.