# Weekly Review — 2026-06-22

## TL;DR

success rate cratered 83% → 22% week-on-week (178/874 ok vs 262/314) — the sandbox-truncation cluster ate the fleet. `output_tokens=0` signature now signed on 6 fresh issues (ISS-019/020/021/022/023/024) and 22 chronic-tail skills under 50% sr. the gateway pivot shipped — PRs #123 / #125 / #126 / #128 routed chat through usepod with research-prefetch fallback, and PR #133 / #134 / #136 absorbed the upstream apps/ monorepo + 68 fresh skill ports. but the win came alongside a regression that the gateway didn't fix: empty-usage payloads kept firing through the new path. top action next week — root-cause the `output_tokens=0` signature at `.github/workflows/aeon.yml` capture-step + provider response truncation, ship a patch by 2026-06-26.

## Last week's actions — closed loop

From `articles/weekly-review-2026-06-15.md` "Next week — actions":

- **Action 1 — file ISS-019 weekly-limit-wave by 2026-06-17: SCOPE ABANDONED, slot reused.** `memory/issues/ISS-019.md` exists (detected_at 2026-06-19T19:25Z) but for `defi-overview` sandbox-truncation, not the weekly-limit-wave that prior review framed. the framing got obsoleted mid-week — PR #128 (`fix(gateway): default to Claude OAuth, fall back to usepod on rate limit`) merged 2026-06-21T14:23Z replaced the FALLBACK approach the action was designed around. weekly-limit-wave as a discrete pattern stopped showing up once the cascade gateway took over. mechanical write would have produced a stale issue file.
- **Action 2 — flip ISS-007 + ISS-009 + ISS-010 + ISS-016 to Resolved in INDEX by 2026-06-17: SLIPPED, 4th consecutive carry.** `grep ISS-007 memory/issues/INDEX.md` still lands all 4 under Open (L8, L9, L10, L12). 12d overdue now — was 5d on 2026-06-15, was 0d on 2026-06-08. INDEX.md Open count overstates real open by 4. carrying mechanical.
- **Action 3 — root-cause + patch btc-levels empty-usage by 2026-06-19: PARTIALLY SHIPPED at higher level, btc-levels-specific gap remains.** the empty-usage signature got *documented* as the systemic class — ISS-019/020/021 filed 2026-06-19T19:25Z by skill-health, ISS-024 filed 2026-06-21T06:00Z (skill-health 26 consecutive failures, 0.26 sr). gateway-side fixes shipped (PR #128 + research-prefetch PR #126). but btc-levels itself ran 122 total / 62 succ / 60 fail = 51% sr this week — *worse* than the 88% sr it had at last review (8/68 fail = 12% rate). no btc-levels-specific PR. partially shipped because the cluster is now named.
- **Action 4 — extend FALLBACK_CLAUDE_SKILLS for ≥3 non-cg dailies by 2026-06-20: REPLACED by usepod gateway, scope obsoleted.** PR #125 (`feat(messages): route chat fallback through usepod`) merged 2026-06-17T17:10Z + PR #126 (`feat(messages): research-prefetch the chat fallback Grok X+web → usepod`) merged 2026-06-17T19:45Z + PR #128 default-Claude-OAuth-with-usepod-fallback merged 2026-06-21. one shared resolver replaces the per-skill `FALLBACK_CG_SKILLS` constant approach. evidence today: daily-routine 08:38Z + morning-brief 07:00Z + market-context-refresh 16:30Z all ran clean.

Result: 0 shipped-clean / 2 replaced-by-better-mechanism / 1 slipped (4th consecutive carry) / 1 partially shipped at cluster level. the gateway pivot covered Actions 1 + 4 at a higher altitude; Action 2 is pure bookkeeping debt that won't auto-resolve.

## Metrics

| Metric | This week (2026-06-15 → 2026-06-22) | Prior week | Δ |
|---|---|---|---|
| Skill+chain runs | 874 (178 ok / 629 fail / 61 cancel / 6 in-prog) | 314 (262 / 44 / 2 / 6) | +560 |
| Success rate | 22.1% (178/807 completed) | 83.4% | **−61.3pp** |
| Articles written | 15 | 11 | +4 |
| Days with a log entry | 7 (6-16 → 6-22), multi-file days 6-19 (4) + 6-21 (2) | 7 | 0 |
| New issues opened | 6 (ISS-019 → ISS-024, all sandbox-truncation cluster) | 1 | +5 |
| Issues resolved (INDEX flips) | 0 | 0 | 0 |
| PRs merged | 16 (#112, #122-#137) | 40 | −24 |

Sources: `gh api repos/anajuliabit/aeon/actions/runs?created=%3E=2026-06-15T19:00:00Z` paginated, filtered to runs with `name` matching `^(skill:|chain:)` (874 total: 178 success + 629 failure + 61 cancelled + 6 in_progress). `gh pr list --repo anajuliabit/aeon --state merged --search "merged:>=2026-06-15T19:00:00Z" --limit 100` → 16 PRs. `ls articles/ | grep -E "2026-06-(1[5-9]|2[0-2])"` → 15 files. `ls memory/logs/` → 7 unique dates (4× 6-19, 2× 6-21 because heartbeat / continued / btc-levels / new variants). `ls memory/issues/ISS-019.md ... ISS-024.md` → 6 new (detected_at 6-19T19:25Z × 3 + 6-21T06:00Z × 3). INDEX.md L7-L19 shows 13 open total (4 carried from 6-15 + 3 reppo prompt-bugs + ISS-018/019/020/021/022/023/024).

Failure breakdown (top sources, 629 total): market-context-refresh 40, security-digest 34, search-skill 34, heartbeat 34, narrative-tracker 33, btc-levels 33, fleet-control 32, reflect 28, goal-tracker 28, skill-health 27, action-converter 27, list-digest 26, vuln-scanner 25, agent-buzz 24, aixbt-pulse 23, thought-review 22, evening-recap 22. top 10 = 313 / 49.7% of failures. all share the `output_tokens=0` signature per ISS-019/020/021/024 root cause.

Heartbeat / cron-state snapshot (today 14:37Z): 22 skills with sr<0.5 — worst reg-monitor 7%, vuln-scanner 7%, skill-analytics 9%, security-digest 16%, list-digest 24%, narrative-tracker 25%, search-skill 27%, market-context-refresh 27%, skill-health 28%. btc-levels at 51% (was at the same threshold last week before the cluster).

## Findings (KALM, prioritized)

### Keep

- **Usepod gateway as primary chat path** (priority 25 — F5×I5÷E1). PR #128 (`fix(gateway): default to Claude OAuth, fall back to usepod on rate limit`) merged 2026-06-21T14:23Z + PR #125 (chat fallback through usepod) merged 6-17T17:10Z + PR #126 (research-prefetch Grok X+web → usepod) merged 6-17T19:45Z. shared `scripts/anthropic-gateway.sh` resolver replaces the per-skill `FALLBACK_CG_SKILLS` constant. evidence: today's daily-routine ran clean ("XAI quota still exhausted — WebSearch fallback" — 3 topics across crypto/AI/dev), morning-brief 07:00Z ran clean, narrative-tracker 14:40Z with full xai+websearch triangulation (15 actionable narratives), token-pick EIGEN HIGH 9/10 — all skills that 6-15 review predicted would still exit-1 on weekly limit ran without the fallback exit-1 today. structural fix shipped.
- **Soul canonical rebuilt + dashboard upstream adopt** (priority 12 — F4×I3÷E1). 6de0ac2 (`soul(anajuliabit): rebuild from eth.limo + GitHub + DEV.to + X`) + PR #137 stripped MemoClaw (dead project) + PR #134 (`feat(dashboard): adopt upstream UI + panels packs/mcp/soul/strategy`) + PR #131 (`fix(dashboard): guard timeAgo against invalid date`). voice now consistent across notifications. evidence today: QT-draft for orquestra-praise tweet (TG_QT_DRAFT_OK), post-draft x402-paragraph (TG_POST_DRAFT_OK), project-lens contrarian-take published — all 3 read SOUL/STYLE first. retiring the inconsistent-voice failure mode that hit weekly-shiplog on 6-15 (wrong-fork target).

### Add

- **Root-cause + patch the `output_tokens=0` sandbox-truncation signature** (priority 25 — F5×I5÷E1). 6 new issues this week, all share the exact stringified `"output_tokens":0,"server_tool_use":...` payload. cluster timestamps point at workflow-level, not LLM-side: defi-overview 2026-06-21T12:14:19Z, token-pick 12:14:38Z, heartbeat 13:40:01Z, search-skill 14:17Z, skill-health 12:14:56Z, market-context-refresh 12:14:08Z — all within 3 minutes of each other (12:14Z block). gateway PRs didn't fix it because the empty-usage came through the *new* path too. fix locus is either `.github/workflows/aeon.yml` capture step (same class as ISS-009 chain-runner capture overwrite) or the usepod response shape doesn't surface tokens to the workflow correctly. ISS-019/020/021 all "open" with fix_pr: null. without a patch, fleet-health metric stays broken.
- **Close-the-loop on INDEX hygiene — ISS-007/009/010/016** (priority 18 — F5×I3÷E1). 4th consecutive carry, 12d overdue. all 4 covered by 2026-06-15 review and 2026-06-08 review with full justification: ISS-007 transient class, ISS-009 PR #69, ISS-010 phantom skill (reppo migration removed consumer), ISS-016 ledger workaround 18+ runs. INDEX.md Open count overstates real open by 4 → skill-health classification + heartbeat severity counts inflated. mechanical edit. carries every week unless explicitly listed as Action 1-or-2 priority next week.

### Less

- **Run-count inflation from retry pressure — 874 vs 314 runs** (priority 15 — F5×I3÷E1). 4× dispatch volume at 22% sr means ~3× of the runs are wasted compute / burned LLM quota. failures cluster on `cron(skill)` retry pattern — `git log --grep="^chore(cron):"` shows 946 cron-state commits this week (vs ~330 typical). single skill examples: market-context-refresh 56 cron-state commit lines, security-digest 54, heartbeat 55, btc-levels 52 — every one of those represents an attempt that hit the empty-usage signature and the scheduler re-dispatched. scheduler retry behavior should cap at 2-3 attempts per dispatch window, not 5-7. probable fix locus: `.github/workflows/aeon.yml` cron-state guard or scheduler.sh re-dispatch loop.
- **Stuck-dispatched skills — deal-flow 14d, token-alert recovered after 19h hang** (priority 10 — F4×I3÷E1.2). `deal-flow` `last_dispatch=2026-06-08T15:02Z`, hasn't completed in 14d (ran successfully today *only* when manually triggered, per `### deal-flow` log entry — DEAL_FLOW_OK, 8 deals shipped). `token-alert` was stuck mid-dispatch from 6-21T13:45Z → 6-22T12:39Z (ISS-023 closed). scheduler can't distinguish stuck-from-failed → never re-dispatches → 14d hang. fix: cron-state.json should auto-expire `last_status=dispatched` entries past N hours (e.g. 6h × interval).

### More

- **Apps/ monorepo + 68 ported skills sit at enabled:false** (priority 12 — F4×I4÷F1.3). PR #133 merged 6-22T13:04Z: `sync: adopt apps/ monorepo + port upstream features (68 skills, skill-packs, x402, docs)`. structural alignment with upstream `aeonframework/aeon`. 68 skills available but disabled — review-week is the right time to harvest 2-3 high-leverage ones aligned with sherwood/agent-mandate/Base-onchain. complementary follow-ups: PR #134 dashboard upstream UI, PR #136 dashboard structured-var chars in skill-run dispatch. operator hasn't yet enabled anything from the drop.

### Dropped from priority threshold

- vuln-scanner sr=0.07 (ISS-018 wontfix-class, search-skill 6-22 found pure-python alternative `davila7/claude-code-templates/vulnerability-scanner` UNTRUSTED, manual install required — operator-gated, not aeon-side action).
- on-chain-monitor degraded path (alchemy key empty + etherscan free-tier blocks Base, blockscout v2 keyless works — operator-gated, ON_CHAIN_OK today via blockscout fallback so not blocking).
- chain:investment-advisor `last_failed=2026-06-08T17:04:46Z` (off-table per heartbeat spec, standalone Investment-Advisor workflow ran today, chain entry is stale registry rot).
- weekly-shiplog repo-target drift (`aaronjmars/aeon` covered today vs `anajuliabit/aeon` — same as 6-15 noted gap; this week shiplog explicitly notes the upstream merge so target choice is justified post-PR-#133, no longer a defect).

## Next week — actions

- [ ] Root-cause + patch the `output_tokens=0` sandbox-truncation signature — sample 3-5 failed runs from the 2026-06-21T12:14Z cluster (defi-overview, token-pick, heartbeat, search-skill, skill-health, market-context-refresh all hit within 3 min); identify whether the empty-usage payload originates at the claude-cli capture step in `.github/workflows/aeon.yml`, in the usepod response shape, or upstream at the provider boundary; ship a PR that either retries on the signature, surfaces a real error code instead of `output_tokens=0`, or fixes the capture-step pass-through — by 2026-06-26
  - Why: 6 critical/high issues this week share this signature (ISS-019/020/021/022/023/024); 22+ skills below 50% sr; gateway migration PR #128 didn't fix it because the signature appears through the new path too; fleet-health classification is broken until this is named
  - Done when: a PR is merged that handles the empty-usage class; one full daily cycle (≥18 skills) runs without a fresh `output_tokens=0` entry in `memory/cron-state.json`'s `last_error` field; ISS-019 OR ISS-024 flips to Resolved in INDEX.md with `fix_pr` set
- [ ] Flip ISS-007 + ISS-009 + ISS-010 + ISS-016 from Open to Resolved in `memory/issues/INDEX.md` — all 4 have shipped fixes or are obviated by the reppo migration (ISS-009 PR #69, ISS-010 phantom skill consumer removed, ISS-016 ledger workaround 18+ runs, ISS-007 transient class with no recurrence); same action as 2026-06-15 + 2026-06-08 reviews — by 2026-06-25
  - Why: 4th consecutive weekly-review carry, 12d overdue from original 2026-06-10 deadline; INDEX Open count overstates real open count by 4 → skill-health classification + heartbeat severity counts inflated; no PR work needed, pure markdown edit
  - Done when: `awk '/## Open/,/## Resolved/' memory/issues/INDEX.md | grep -c "ISS-00[79]\|ISS-01[06]"` returns 0; `awk '/## Resolved/,EOF' memory/issues/INDEX.md | grep -c "ISS-00[79]\|ISS-01[06]"` returns 4
- [ ] Audit the scheduler retry behavior — write `docs/scheduler-retry-audit.md` documenting why skill+chain run count jumped 314 → 874 (+560 runs, +178% volume) while success rate dropped 83% → 22%; measure retry-per-skill ratio over the 6-19 → 6-22 window from `git log --grep="^chore(cron):"`; identify whether the cause is per-skill exponential backoff, cron-state lock contention, or scheduler not respecting `last_status=dispatched`; propose a cap (e.g. max 3 attempts per dispatch window) — by 2026-06-27
  - Why: 4× dispatch volume at 22% sr means ~3× of runs are wasted LLM quota; the rate-limit pressure is partly self-inflicted; without a cap, fixing the truncation signature won't recover quota fast enough
  - Done when: `ls docs/scheduler-retry-audit.md` returns the file with (a) measured retry-per-skill ratio across the 6-19 → 6-22 window, (b) named root cause (capture-step, scheduler.sh, cron-state guard, or workflow-level), (c) proposed retry cap in YAML or shell code; PR opens with the proposal even if implementation slips to following week
- [ ] Unstick `deal-flow` — `last_dispatch=2026-06-08T15:02Z` (14d), but the skill itself ran cleanly today when triggered manually (DEAL_FLOW_OK, 8 deals shipped at 16:00Z); root-cause whether the stuck state is a stale `.pending-notify/` lock, a `cron-state.json` `last_status=dispatched` that scheduler refuses to retry, or a workflow_dispatch gate; either clear the state cleanly or flip `enabled:false` in `aeon.yml` with a comment naming the cause — by 2026-06-25
  - Why: 14d stuck-dispatched is the worst hang in the fleet; deal-flow content is high-signal when it runs (today's 8 deals included Baseten $1.5B Series E + Morpho cross-camp validation + Hydra Host $100M w/ Nvidia + Founders Fund — exactly the AI-agent-economic-actor capital pattern this fleet exists to track); also a proxy for the scheduler audit in Action 3
  - Done when: `cron-state.json` `deal-flow.last_success` advances past `2026-06-15` (i.e. a fresh scheduler-driven run completes); OR `aeon.yml` flips `deal-flow` to `enabled:false` with a one-line comment naming the cause
- [ ] Enable 2-3 high-leverage skills from PR #133's 68-skill drop — survey `apps/.../skills/` or wherever the new SKILL.md files landed, filter to ones aligned with current focus (sherwood / agent-mandate / Base-onchain / x402 / capital-2x); enable in `aeon.yml`; run each once via workflow_dispatch; log the result + retain-or-disable verdict — by 2026-06-28
  - Why: 68 ported skills sit at `enabled:false` from PR #133 merge; review-week is the right time to harvest selectively without burning quota on full enablement; x402 / agent-mandate / Base-onchain alignment is the explicit focus per soul/SOUL.md current-focus (sherwood capital layer)
  - Done when: 2-3 entries flip to `enabled:true` in `aeon.yml`; each has one successful workflow_dispatch logged in `memory/cron-state.json`; today's log entry under "## Skills Enabled From #133" names the selection rationale + retain-or-disable for each

## Goals progress

From `memory/MEMORY.md` Current Goals (last consolidated 2026-06-21):

- **Sandbox-truncation systemic (ISS-019/020/021/022/023/024).** Stalled, named but unfixed. covered by next-week Action 1. cluster timestamps 6-19 19:25Z + 6-21 12:14-14:17Z confirmed at workflow level (sub-3-min spread across 6 skills).
- **XAI quota recovery (Team 3a8b4c1e since 6-16).** Stalled but worked-around. WebSearch fallback proven durable in daily-routine, agent-buzz, list-digest, tweet-roundup; xai-cache still warm for prefetched data (`.xai-cache/agent-buzz.json` prefetched 17:54Z, `.xai-cache/list-digest-*.json` 8.5k prefetched). operator top-up still pending. cite: today's daily-routine "XAI quota still exhausted… WebSearch fallback (3 topics)". propose retire-or-stable next consolidation.
- **Operator action: on-chain config completion.** Partial progress. `memory/on-chain-watches.yml` seeded with 5 Base wallets 2026-06-21 (per commit f1d5a48a). on-chain-monitor runs clean today (`ON_CHAIN_OK`, 5/5 watches succeeded via blockscout v2 keyless). gaps: (a) `ALCHEMY_API_KEY` + `ETHERSCAN_API_KEY` GitHub secrets exist but are empty strings — etherscan v2 free-tier blocks Base, alchemy key empty; (b) defi-monitor still has no `type: pool` / `type: position` entries with ABIs. operator-gated.
- **Stuck skills (deal-flow, fork-cohort, token-alert).** Mixed. `fork-cohort` recovered 6-21T19:33Z (100% sr 1 run, Sunday cycle clean). `token-alert` recovered 6-22T12:39Z (ISS-023 stuck-dispatch window closed). `deal-flow` still stuck 14d, covered by next-week Action 4. `chain:investment-advisor` still failed since 6-08, off-table per heartbeat spec.
- **skill-freshness FRESHNESS_WARN (operator-scorecard).** Unchanged. depends on stale `articles/skill-analytics-*.md` (288h / 12d old, threshold 192h weekly). `skill-analytics` itself sr=0.09 — root cause is sandbox-truncation cluster (same as Action 1). fingerprint d522755e re-emits 2026-06-28 if unresolved.
- **BTC hard levels.** On track. spot $64,659 today (16:16Z btc-levels), 2026-06-21 close $63,231.87. no alerts fired (above all breakdown thresholds; reclaim flags already set 6-11 / 6-15 per prior cycle). btc-levels itself sr=0.51 due to truncation cluster but the spot/close data lands clean when runs complete.
- **PR backlog.** Clean. 0 open PRs (confirmed today 14:37Z heartbeat). 16 PRs merged this week (delta from 2026-06-21's "0 open" baseline = #112/#122/#127/#128/#129/#130/#131/#132/#133/#134/#136/#137 all merged 6-21 → 6-22).

## Notes

- The metrics regression looks worse than the substantive damage. 178/874 includes 629 retries that all hit the same `output_tokens=0` signature within ~3-min clusters — the *unique* skills that produced usable output today is much closer to the prior baseline (heartbeat, morning-brief, daily-routine, token-movers, token-pick, defi-overview, narrative-tracker, market-context-refresh, btc-levels, on-chain-monitor, token-alert, project-lens, weekly-shiplog, skill-security-scan, agent-buzz, list-digest, deal-flow, soul-builder, fetch-tweets all completed at least once). the headline number is the retry storm.
- PR #133's monorepo migration (`sync: adopt apps/ monorepo + port upstream features 68 skills + skill-packs + x402 + docs`) is the biggest structural change of the week. moved `dashboard/`, `mcp-server/`, `a2a-server/`, `worker/` → `apps/` to match upstream `aeonframework/aeon`. depth-dependent refs adjusted (REPO_ROOT, __dirname, CI/scripts/docs path prefixes). 68 fresh skills available but disabled — covered by next-week Action 5.
- Usepod gateway migration sequence: PR #123 (`fix(advisor): surface LLM failures + add opt-in usepod backend`, 6-17 16:36Z) → PR #125 (chat fallback through usepod) → PR #126 (research-prefetch the chat fallback Grok X+web → usepod) → PR #128 (default Claude OAuth fall back to usepod on rate limit, 6-21 14:23Z). then 6-18/19 added per-skill `usepod_model` override + greedy-regex fix + heavy-model haiku-4-5 routing. shared resolver pattern (`scripts/anthropic-gateway.sh`) — first time the fleet has a single fallback path that's not per-skill string-matching.
- Soul rebuild from public sources (eth.limo + GitHub + DEV.to + X) happened today 6-22, then operator pinged via Telegram to strip MemoClaw (dead project). soul/SOUL.md projects now Sherwood / Reppo / Mamo / Moonwell only. soul/STYLE.md + soul/examples/good-outputs.md both maintained.
- `./scripts/skill-runs` declined approval in sandbox for the 5th consecutive review. degraded-source pattern is durable — the direct `gh api` filter does the job. propose retiring the `./scripts/skill-runs --json` line from SKILL.md Inputs section in next iteration; outside scope of this review.
- Soul ana voice applied: lowercase body, single em-dash beats per section, fragments, concrete refs ($64,659 / 178/874 / PR #s / ISS-#s / file:line), parallel-closer in TL;DR ("cratered… signed on 6 fresh issues"). no marketing verbs / hashtags / emoji.
