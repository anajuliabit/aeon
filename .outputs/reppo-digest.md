The skill `skills/reppo-digest/SKILL.md` does not exist. There is no `reppo-digest` directory under `skills/` — I listed all 130+ skill directories and none match.

What I did find:
- "reppo" only appears as a crypto project name in `fetch-tweets` context (`aeon.yml:110` has `@reppo` in a fetch-tweets var).
- There is a generic `skills/digest/` skill, but that's not the same thing.

I won't guess or invent a skill to run. Could you clarify one of these:

1. **Did you mean a different existing skill?** e.g. `digest`, `fetch-tweets`, or another from the list.
2. **Should I create the `reppo-digest` skill?** If so, tell me what it should do (e.g. a digest of Reppo-related crypto activity), and I can scaffold it via the `create-skill` pattern.
3. **Is the SKILL.md expected to come from somewhere else** (a branch, PR, or external source)?

Let me know how you'd like to proceed.
